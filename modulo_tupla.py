import modulo_retorno

def tuples(integers, reals, words, sentence):
    """
    Aqui veremos como utilizar Tuples.
    """
    print('\nBem vindo(a) à seção de Tuples\n')

    "PLACEHOLDER"

    modulo_retorno.retorno(integers, reals, words, sentence)